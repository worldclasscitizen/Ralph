# Ralph 사용자 개입 메모

필요할 때 이 문서를 `OPERATOR_NOTE.local.md`로 복사하고, 다음 AI 단계부터 반영할 지시를 작성한다.

예시:

- 현재 변경 중 결제·배포·외부 메시지 전송은 하지 않는다.
- 모바일 UI보다 데스크톱 핵심 사용자 흐름을 먼저 완성한다.
- 방금 발견한 `tests/auth.test.ts` 실패를 다음 반복의 최우선 원인으로 다룬다.

지시가 더 이상 필요 없으면 본문을 비운다. 이미 실행 중인 한 번의 agent 호출을 즉시 바꾸지는 못하며 다음 단계부터 적용된다.
